# frozen_string_literal: true

# poetry configuration. Defaults shown commented.
#
# Poetry::Core::Config.current.css_mode = :tailwind   # the default for component kits YOU write on the
#                                                      # DSL (:bem = bring your own CSS); poetry-ui is
#                                                      # Tailwind-native and pins its own, so this never
#                                                      # reaches it
# Poetry::Core::Config.current.icon_library = :lucide
