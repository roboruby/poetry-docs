<%= turbo_frame_tag(request.headers["Turbo-Frame"] || "fragment") do %>
  <%= poetry_card do %>
    <div class="flex flex-col gap-1 p-4">
      <p class="text-sm font-medium">Deploy finished</p>
      <p class="text-sm text-muted-foreground">
        poetry-docs · main · build took 42s — this card was fetched at
        <%= Time.current.strftime("%H:%M:%S") %>, the moment the frame became visible.
      </p>
    </div>
  <% end %>
<% end %>
