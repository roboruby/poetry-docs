<%# aria-busy marks the region the loaded content will replace. %>
<div class="flex w-72 flex-col gap-4" aria-busy="true">
  <%= poetry_skeleton(class: "h-40 w-full rounded-xl") %>
  <div class="flex flex-col gap-2">
    <%= poetry_skeleton(class: "h-4 w-3/4") %>
    <%= poetry_skeleton(class: "h-4 w-1/2") %>
  </div>
</div>
