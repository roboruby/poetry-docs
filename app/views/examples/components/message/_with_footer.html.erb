<div class="w-full max-w-md">
  <%= poetry_message do |message| %>
    <% message.with_avatar { "AI" } %>
    <% message.with_footer { "10:41" } %>
    <%= poetry_bubble(variant: :muted) { "Footer present - the avatar lifts." } %>
  <% end %>
</div>
