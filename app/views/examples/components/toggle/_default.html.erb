<%# Icon-only toggles require label: - and it must not change with
    state ("Bookmark", never "Remove bookmark"). %>
<%= poetry_toggle(label: "Bookmark") do %>
  <%= poetry_icon(name: :bookmark) %>
<% end %>
