<div class="flex w-full max-w-md flex-col gap-6">
  <%= poetry_marker(tag: :a, href: "#thread-start", variant: :separator, class: "cursor-pointer") { "Jump to the start of this thread" } %>
  <%= poetry_marker(tag: :button, type: :button, variant: :border, class: "cursor-pointer") { "Load 24 earlier messages" } %>
</div>
