<%# href: renders a real anchor with button styling - navigation stays
    navigation (no onclick handlers). %>
<div class="flex items-center gap-2">
  <%= poetry_button(href: "/charts/area", variant: :outline) { "Go to charts" } %>
</div>
